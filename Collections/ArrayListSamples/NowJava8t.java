package com.expertzlab.Collections.ArrayListSamples;

import com.expertzlab.filehandling.ObjectInputStreamEx;

import java.util.Arrays;
import java.util.List;

/**
 * https://www.mkyong.com/java8/java-8-streams-filter-examples/
 */
public class NowJava8t {
    public static void main(String[] args) {

      /*  List<ObjectInputStreamEx.Person> persons = Arrays.asList(
                new ObjectInputStreamEx.Person("mkyong", 30),
                new ObjectInputStreamEx.Person("jack", 20),
                new ObjectInputStreamEx.Person("lawrence", 40)
        );

        ObjectInputStreamEx.Person result1 = persons.stream()                        // Convert to steam
                .filter(x -> "jack".equals(x.getName()))        // we want "jack" only
                .findAny()                                      // If 'findAny' then return found
                .orElse(null);                                  // If not found, return null

        System.out.println(result1);

        ObjectInputStreamEx.Person result2 = persons.stream()
                .filter(x -> "ahmook".equals(x.getName()))
                .findAny()
                .orElse(null);

        System.out.println(result2);

    }*/
    }
}